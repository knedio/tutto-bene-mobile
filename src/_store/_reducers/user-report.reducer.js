import { userReportConstants } from '_constants';

const initialState = {
    reports: {},
}

export default userReport = ( state = initialState , {type, payload} ) => {
    switch ( type ) {
        default:
            return state;
    }
}