import { userQuarantineConstants } from '_constants';

const initialState = {
    reports: {},
}

export default userQuarantine = ( state = initialState , {type, payload} ) => {
    switch ( type ) {
        default:
            return state;
    }
}